export default class Food {
  /**
   * @param {Number} x
   * @param {Number} y
   * @param {Number} scent
   */
  constructor (x, y, scent) {
    this.x = x
    this.y = y
    this.scent = scent
  }
}
